try:
    age = int(input("enter your age : "))
    if age % 2 == 0 :
            print("your age is even")
    else:
        print("your age is odd")
except ValueError as val:
    print(val, ": value error")
except OverflowError as over:
    print(over, ": overflow error")
