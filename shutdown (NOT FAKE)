import os

def shutdown():
    choose = input("Would you like to shut the system down? ")
    if choose == "yes":
        print("Shutting down... ")
        os.system("shutdown /s /t 1")
    elif choose == "no":
        print("Aborted shutdown.")
    else:
        print("Invalid Input!")
shutdown()
    
